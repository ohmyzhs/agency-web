# Dev Fix Report — 2026-05-10 00:42

## Summary
Fixed the shared client-hydration root cause behind the QA-reported Word Defense loading state and the QR Generator no-output behavior.

## GitHub issue workflow
- Linked/closed issue: #12 — QR and Barcode Generator: Generate QR produces no visible output or download controls
- New broader/Word Defense issue creation attempted but blocked by GitHub EMU authorization:
  - `gh issue create`: `GraphQL: Unauthorized: As an Enterprise Managed User, you cannot access this content (createIssue)`
  - REST `POST /repos/ohmyzhs/agency-web/issues`: HTTP 403 with the same EMU message
- Attempted to comment on #12 with the shared-root-cause note, but `addComment` was also blocked by the same EMU authorization.
- Saved issue body for the broader hydration bug: `.hermes/qa/issue-app-provider-hydration-body.md`

## Bugs fixed
1. Word Defense remains stuck on loading state
   - QA report: `.hermes/qa/2026-05-10-0001-qa-report.md`
   - Root cause: `AppProvider` used `useSyncExternalStore` with `getMountedClient = () => true` and `getMountedServer = () => false`, while gating real context behind `mounted`. The first browser render did not match the server fallback shell, so React interactivity/client islands were not attached.
   - Fix: make the first client snapshot server-compatible (`false`) and flip `mounted` after hydration in a microtask scheduled from an effect.

2. QR Generator click produces no visible output/download controls
   - GitHub issue: #12
   - Same root cause: buttons were visible but React handlers were not attached under the broken provider hydration path. After fixing provider hydration, QR generation renders the image and SVG/PNG download controls.

## Files changed
- `src/components/providers.tsx`
- `src/components/providers.client-ready.test.mjs`
- `.hermes/qa/issue-app-provider-hydration-body.md`

## Tests and verification run
- `node --test src/components/providers.client-ready.test.mjs` — PASS
- `npm run lint` — PASS
- `npx tsc --noEmit --pretty false` — PASS
- `npm run build` — PASS
- Production smoke server: `npm run start -- --hostname 127.0.0.1 --port 3012`
- Browser smoke checks against production build:
  - `/typing/game/word-defense`: fallback `게임 로드 중…` absent; `시작하기` visible.
  - `/tools/qr-barcode-generator`: after clicking QR 생성, QR image plus SVG/PNG download controls visible.

## Commit
- SHA: `237696b`
- Message: `Fix AppProvider client hydration`
- Commit body: `Fixes #12`
- Pushed: yes, `origin main`

## Deferred / notes
- GitHub issue creation/commenting from this environment is blocked by account-level Enterprise Managed User authorization, despite repo/token access for reads and git push. Future QA issue creation may require a different GitHub actor/token or org settings change.
